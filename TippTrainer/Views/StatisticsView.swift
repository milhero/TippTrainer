import SwiftUI

/// Platzhalter — wird in Phase 6 durch die sechs Auswertungsansichten ersetzt.
struct StatisticsView: View {
    var body: some View {
        ContentUnavailableView(
            "Lernstatistik",
            systemImage: "chart.xyaxis.line",
            description: Text("Deine Auswertungen erscheinen hier.")
        )
    }
}
